"""微信适配器常量集中定义

将所有配置常量集中在一个文件中，便于维护和修改。
"""

# ---------------------------------------------------------------------------
# API 基础 URL
# ---------------------------------------------------------------------------

DEFAULT_BASE_URL = "https://ilinkai.weixin.qq.com"
DEFAULT_CDN_BASE_URL = "https://novac2c.cdn.weixin.qq.com/c2c"

# ---------------------------------------------------------------------------
# 超时设置（毫秒）
# ---------------------------------------------------------------------------

LONG_POLL_TIMEOUT_MS = 35000
DEFAULT_API_TIMEOUT_MS = 15000
DEFAULT_CONFIG_TIMEOUT_MS = 10000

# ---------------------------------------------------------------------------
# 会话管理
# ---------------------------------------------------------------------------

SESSION_PAUSE_MS = 300000  # 会话过期暂停时长：5 分钟
SESSION_EXPIRED_ERRCODE = -14  # 会话过期错误码

# ---------------------------------------------------------------------------
# 重试配置
# ---------------------------------------------------------------------------

MAX_CONSECUTIVE_FAILURES = 3  # 最大连续失败次数
BACKOFF_DELAY_MS = 30000       # 失败退避时长：30 秒
RETRY_DELAY_MS = 2000         # 失败重试间隔：2 秒

# ---------------------------------------------------------------------------
# 认证配置
# ---------------------------------------------------------------------------

DEFAULT_ILINK_BOT_TYPE = "3"        # 默认 bot 类型
LOGIN_TIMEOUT_MS = 300000            # 登录超时：5 分钟
LOGIN_MAX_REFRESH_COUNT = 3          # 二维码最大刷新次数
ACTIVE_LOGIN_TTL_MS = 5 * 60_000     # 登录会话存活时间：5 分钟
QR_LONG_POLL_TIMEOUT_MS = 35000      # 二维码状态轮询超时

# ---------------------------------------------------------------------------
# 上下文缓存配置
# ---------------------------------------------------------------------------

DEFAULT_CONTEXT_TTL = 7200          # context_token 默认存活时间：2 小时
CONTEXT_CLEANUP_INTERVAL = 300       # 上下文缓存清理间隔：5 分钟

# ---------------------------------------------------------------------------
# 消息类型
# ---------------------------------------------------------------------------

class MessageType:
    """消息类型"""
    NONE = 0
    USER = 1
    BOT = 2


class MessageItemType:
    """消息条目类型"""
    NONE = 0
    TEXT = 1
    IMAGE = 2
    VOICE = 3
    FILE = 4
    VIDEO = 5


class MediaType:
    """媒体类型"""
    IMAGE = 1
    VIDEO = 2
    FILE = 3
    VOICE = 4


class MessageState:
    """消息状态"""
    NEW = 0
    GENERATING = 1
    FINISH = 2


class TypingStatus:
    """输入状态"""
    TYPING = 1
    CANCEL = 2


class QrcodeStatusType:
    """二维码状态类型"""
    WAIT = "wait"           # 等待扫码
    SCANED = "scaned"       # 已扫码
    CONFIRMED = "confirmed" # 已确认
    EXPIRED = "expired"     # 已过期


# ---------------------------------------------------------------------------
# 渠道版本
# ---------------------------------------------------------------------------

CHANNEL_VERSION = "1.0.0"
